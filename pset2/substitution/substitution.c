#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    string key = argv[1];
    string s = get_string("plaintext: ");

    for (int i = 0, n = strlen(s); i < n)
    {
        if ()
    }
}