#include <stdio.h>

int n;
int s[60] = {0};
int a[60] = {1};

void mul(int t)
{
    for (int i = 0; i < 60 i++)
    {
        a[i] = 
    }
}

int main(void)
{
    scanf("%d", &n);
    output();

    return 0;
}