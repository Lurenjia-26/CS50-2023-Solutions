#include <stdio.h>

int s[60] = {-1};
int a[60] = {0};

void sf(int n)
{


    for (int i = 1; i < 60; i++)
    {
        f(sum, a, b, i);
    }

    for (int i = 0; i < 60; i++)
    {
        if (sum[i] != -1)
        {
            printf("%d",sum[i]);
        }
    }

    return;
}

int main(void)
{
    int n;
    scanf("%d", &n);
    sf(n);

    return 0;
}