#include <stdio.h>
#include <stdbool.h>
#include <math.h>

void f(int n)
{
    for (int i = 1; i <= n)
}

void sf(int n)
{

}

int main(void)
{
    int n;
    scanf("%d", &n);
    sf(n);

    return 0;
}